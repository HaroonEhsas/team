buildscript {
    repositories {
        google()  // Add this line if not present
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.3.1'
        classpath 'com.google.gms:google-services:4.4.2'  // Ensure this line is added
    }
}
